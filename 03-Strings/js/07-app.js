const producto = 'Monitor de 20 pulgadas';

console.log(producto.toUpperCase());
console.log(producto.toLowerCase()); //usar antes de guardar los emails en la BD

//Convertir un número en string
const precio = 300;
console.log(precio);
console.log(precio.toString());

//En la consola, el número se ve azul, y el string en negro



