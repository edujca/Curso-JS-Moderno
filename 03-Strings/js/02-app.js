const producto = 'Monitor de 20"';

//Métodos de los strings

console.log(producto.length);

console.log(producto.indexOf('de')); //número en que comienza la palabra

console.log(producto.includes('de')); //devuelve truo o false

