//Un array es para agrupar elementos

//Usan corchetes y sólo tienen valores (no llaves)

const numeros = [10,20,30];
console.log (numeros);

const meses = new Array ('Enero', 'Febrero', 'Marzo');
console.log (meses);

//Se puede crear un arreglo de un elemento, y d elementos variados

//También puede haber un array dentro de otro

const deTodo = [ 'Hola', 10, true, 'si', null, {nombre: "Marilyn", trabajo: 'Actriz'}, [1,2,3] ];

console.log(deTodo);