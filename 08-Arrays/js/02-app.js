const numeros = [10,20,30, 40,50, 60, [1,2,3]];
console.log (numeros);

//console.table crea una tabla con índices y valores
console.table (numeros);


//Acceder a una determinada posición
//Si no existe, devuelve undefined

console.log(numeros[3]);
console.log(numeros[6][1]); //devuelve 2, la posición 6, subposición 1

